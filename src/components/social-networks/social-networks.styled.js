import styled from "@emotion/styled"

export const DivWrapper = styled.div`
  text-decoration: inherit;
`

export const LinkWrapper = styled.a`
  font-size: ${p => p.iconSize}rem;
  color: inherit;
  margin-right: 0.5rem;
`
